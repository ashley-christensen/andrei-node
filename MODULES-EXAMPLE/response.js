function decrypt(data) {
 //library that implements TLS, protocol browsers/servers use to send encrypt. data over web
 return 'decrypted data';
}

function read() {
 return decrypt('data');
}

module.exports = {
 read,
};